version: '3.8'

services:
  web:
    build: .
    ports:
      - "${HOST_PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
      - ./static:/app/static
    restart: unless-stopped
